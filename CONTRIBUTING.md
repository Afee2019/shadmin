# Contributing to Shadmin

感谢你对 **Shadmin** 项目的关注！本指南将帮助你设置开发环境并了解如何贡献代码。

---

## 概述

本项目基于 **Next.js 16**、**TypeScript**、**Tailwind CSS v4** 和 **Shadcn UI** 构建。
目标是保持代码库模块化、可扩展且易于维护。

---

## 项目结构

我们采用 **colocation（共置）** 架构，每个功能模块包含自己的页面、组件和逻辑。

```
src
├── app               # Next.js 路由 (App Router)
│   ├── (external)    # 外部/公开页面
│   └── (main)        # 主应用路由
│       ├── auth      # 认证页面
│       └── dashboard # 仪表盘页面
├── components        # 共享 UI 组件
├── hooks             # 可复用 Hooks
├── lib               # 配置 & 工具函数
├── stores            # Zustand 状态管理
├── styles            # Tailwind / 主题设置
└── types             # TypeScript 类型定义
```

---

## 快速开始

### Fork 并克隆仓库

1. **Fork 仓库**

   点击 [这里](https://github.com/Afee2019/shadmin/fork) Fork 本仓库。

2. **克隆仓库**

   ```bash
   git clone https://github.com/YOUR_USERNAME/shadmin.git
   ```

3. **进入项目目录**

   ```bash
   cd shadmin
   ```

4. **安装依赖**

   ```bash
   pnpm install
   ```

5. **启动开发服务器**

   ```bash
   pnpm dev
   ```

   访问 [http://localhost:3000](http://localhost:3000) 查看效果。

---

## 贡献流程

- 开始工作前，请创建新分支：

  ```bash
  git checkout -b feature/my-update
  ```

- 使用清晰的提交信息：

  ```bash
  git commit -m "feat: add finance dashboard screen"
  ```

- 准备好后，提交 Pull Request。
- 如果你的更改添加了新的 UI 页面或组件，请在 PR 描述中附上截图。

---

## 贡献方向

- **外部页面**: 落地页或其他非仪表盘路由 → `src/app/(external)/`
- **认证页面**: 登录、注册等 → `src/app/(main)/auth/`
- **仪表盘页面**: CRM、Finance、Analytics 等 → `src/app/(main)/dashboard/`
- **组件**: 可复用 UI → `src/components/`
- **Hooks**: 自定义逻辑 → `src/hooks/`
- **主题预设**: 新预设 → `src/styles/presets/`

---

## 代码规范

- 优先使用 **TypeScript 类型**，避免 `any`
- Husky pre-commit hooks 已启用 - 提交时自动运行 lint 和格式化
- 遵循 **Shadcn UI** 风格和 Tailwind v4 规范
- 注意可访问性 (ARIA, 键盘导航)
- 使用清晰的提交信息，带有约定前缀 (`feat:`, `fix:`, `chore:` 等)
- 避免不必要的依赖 - 尽可能使用现有工具

---

## 提交 PR

- 更改准备好后，提交 Pull Request。
- 确保你的分支与 `main` 保持同步。
- 在 PR 中引用相关 Issue。

---

## 问题 & 支持

- 通过 [GitHub Issues](https://github.com/Afee2019/shadmin/issues) 报告 bug、建议或问题

---

感谢你的贡献！🚀
